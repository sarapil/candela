# Candela — Entity Relationship Diagram
# كانديلا — مخطط العلاقات

> 55 DocTypes

```mermaid
erDiagram
    CDSettings ||--|| CDSettings : singleton
```

> **Note:** This is a placeholder ERD. Update with actual DocType relationships from the JSON definitions.
> Run: `ls candela/candela/*/doctype/` to discover all DocTypes and their Link fields.
